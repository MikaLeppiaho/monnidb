A database built as a tool to learn the c++ language and how databases work

Example queries
CREATE TABLE users (id INT, name STRING)
INSERT INTO users (id, name) VALUES (1, 'Alice')
SELECT \* FROM users
